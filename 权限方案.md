# 细粒度权限方案

> 登录/注册 + 两级角色（admin/user）已落地。本文记录如何把权限从硬编码两级，
> 升级为 admin 可在 UI 里按用户配置的细粒度权限。

## 1. 现状与问题

| 层 | 现状 | 问题 |
|---|---|---|
| 数据 | `users.role ∈ {admin, user}` | 只有「管理员 / 普通」两档 |
| 后端 | `require_user`（登录）+ `require_admin`（role=admin） | 权限 = 一个布尔值，加维度要改代码 |
| 前端 | `user.role === 'admin'` 硬判 | 每加一种权限到处改条件 |

结果：表达不了「能分诊但不能改配置」「能改配置但不能清库」这类组合。

## 2. 权限键（固定命名空间）

4 个字符串键，admin 隐式拥有全部，user 按列表判断：

| key | 含义 | 覆盖路由 |
|---|---|---|
| `triage` | 分诊动作 | `disposition`、`restore`、`patch_case`、`false/true-positive`、`push`、`bulk-false-positive`、`tolerance/innate remove+clear` |
| `config` | 配置写 | `knob / mode / model / detection / ingest / sources / webhooks / freq / gating / presets` |
| `maintenance` | 维护 | `reset`、`consolidate` |
| `users` | 用户管理 | `register / users list/delete/update-role/reset-password/set-permissions` |

- **读接口**（所有 GET）保持 `require_user`：登录即可看，不设 `view` 键。
- `report/export` 保持 `require_user`（只读生成）。
- 机器入库 `X-API-Token` 不在本体系。

## 3. 数据模型

```sql
-- users 表加一列（JSON 数组，复用 entity_summary 的 JSON 列模式）
ALTER TABLE users ADD COLUMN permissions TEXT DEFAULT '[]';
```

`role='admin'` = 隐式全权限（校验直接放行）；`role='user'` = 按 `permissions` 判断。

## 4. 后端实现

**`auth.py`** —— 目录常量 + 依赖工厂：

```python
PERMISSIONS = {"triage": "分诊…", "config": "配置…", "maintenance": "维护…", "users": "用户管理"}

def require_perm(perm: str):
    def _dep(user: dict = Depends(require_user)) -> dict:
        if user.get("role") == "admin" or perm in (user.get("permissions") or []):
            return user
        raise HTTPException(403, f"缺少权限：{perm}")
    return _dep
```

`require_user` 返回的 user 里把 `permissions` 解析成 list。

**路由挂载**（`dependencies=[Depends(auth.require_perm("…"))]`）：
- `cases.py` 写接口 → `require_perm("triage")`。
- `dashboard.py` 配置写 → `require_perm("config")`；`reset/consolidate/upload` → `require_perm("maintenance")`；`disposition/restore/tolerance/innate` → `require_perm("triage")`。
- `api/auth.py` 用户管理 → `require_perm("users")`；新增 `GET /permissions`（目录）、`PUT /users/{id}/permissions`（写权限列表）。

## 5. 前端实现

- `AuthUser` 加 `permissions: string[]`；`/auth/me`、`/auth/users`、`login` 都返回。
- `src/perm.ts`：`PERMISSIONS` 目录 + `hasPerm(user, key)`（`role==='admin' || permissions.includes(key)`）。
- 用户管理页加「权限」按钮 → 弹窗勾选（triage/config/maintenance/users），保存调 `PUT /users/{id}/permissions`。
- 页面门控：`Settings` 配置卡用 `hasPerm('config')`、维护卡用 `hasPerm('maintenance')`；`Users` 管理区用 `hasPerm('users')`。

## 6. 默认值与迁移

- 存量 `admin` 不动（隐式全权限）；存量 `user` 的 `permissions` 迁移默认 `'[]'`。
- 新建 `user` 角色默认 `["triage"]`（= 能看 + 能分诊）；admin 建号后可改。

## 7. 取舍

- **权限键而非 N 个角色**：角色数量一多就组合爆炸（看+改+清库…）；权限键是位图，admin 勾选即可。
- **admin 隐式全权限**：避免「管理员把自己权限关掉锁死」的经典坑，也少一层配置。
- **不建独立 permissions 表**：JSON 列 + 固定键清单，零 JOIN、可读；键清单写死在后端 `auth.PERMISSIONS`，前端从 `/permissions` 拉展示。
- **暂不做**：按资源实例的细粒度（如只能看某案件）、权限有效期/审批流——那要对象级 ACL，下一个量级。
