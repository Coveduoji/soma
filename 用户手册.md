# 神经免疫 NeuroImmune · 用户手册

> SOC 告警降噪与研判工作台。用「神经系统 + 免疫系统」的隐喻，把海量告警层层压缩成少数需要人工研判的案件，并随使用越用越准。

---

## 目录

1. [产品简介](#一产品简介)
2. [快速开始](#二快速开始)
3. [核心概念与告警旅程](#三核心概念与告警旅程)
4. [页面功能与使用](#四页面功能与使用)
5. [数据接入](#五数据接入)
6. [系统配置（含每个字段含义）](#六系统配置)
7. [权限管理](#七权限管理)
8. [运维与维护](#八运维与维护)
9. [常见问题 FAQ](#九常见问题-faq)

---

## 一、产品简介

**解决什么问题**：安全设备（天眼/HIDS/WAF 等）每天产生海量告警，人看不过来（告警疲劳）。本平台把「规则秒拦 + 白名单降噪 + 便宜模型初筛 + 贵模型深度研判」编排成流水线，把上千条告警压到每天真正需要人看的少数案件。

**三大核心价值**：

| 价值 | 说明 |
|---|---|
| **降噪** | 用固有免疫秒拦 + 免疫耐受白名单 + 便宜初筛模型（杏仁核），把海量告警压到可处理量级，贵的深度模型只醒「真正可疑的那几条」 |
| **越用越准** | 分析师判定回写白名单/黑名单；夜间巩固把当天案件蒸馏成检索记忆，供后续研判参考 |
| **风险档位** | 管理层拨「风险旋钮」，全局换挡（宽松/正常/保守/战时） |

**双术语体系**：界面支持「生物术语 / 安全术语」一键切换（如「丘脑」↔「原始告警」、「免疫」↔「规则库」）。本手册用生物术语并括号标注安全含义。

---

## 二、快速开始

### 1. 一键启动（开发/演示）

```bash
./start.sh
```

自动装依赖 → 起后端 `:8000` → 起前端 `:5173`，日志写入 `logs/`，syslog 接收器监听 `:5514`（UDP + TCP）。

- 访问 http://localhost:5173
- 默认管理员：**admin / admin**（生产环境务必用环境变量覆盖；本地 `.env` 会覆盖此默认值）
- 自定义端口：`BACKEND_PORT=8010 FRONTEND_PORT=5174 ./start.sh`

### 2. 手动启动

```bash
# 后端（backend 目录下，先执行数据库迁移再启动）
cd backend && alembic upgrade head && python3 -m uvicorn app.main:app --port 8000
# 前端（另开终端）
cd frontend && npm install && npm run dev
```

### 3. 容器化部署（生产推荐）

```bash
cp .env.example .env     # 填 NEUROIMMUNE_ADMIN_PASSWORD / NEUROIMMUNE_API_TOKEN
docker compose up -d --build
```

- nginx 托管前端静态 + 反代 `/api`（默认 80 端口，HTTPS 见 `nginx.conf` 注释）。
- 数据持久化在命名卷 `neuroimmune-data`。
- 内置 `kafka` + `filebeat` 服务：Filebeat 采集本机日志目录 → Kafka → 后端消费（见「五、数据接入 → 通道 5」）。
- 备份/恢复：`./scripts/backup.sh` / `./scripts/restore.sh`。
- **安全底线**：未设管理员凭据时**拒绝启动**；`/api/ingest` 未配 token 时**拒绝接入**；syslog 默认只绑 `127.0.0.1`。

### 4. 纯内网离线部署

在能上网的机器上打包，拷入无网络的服务器：

```bash
./scripts/offline_bundle.sh            # Docker 方案：镜像 tar.gz → 内网 docker load
./scripts/offline_bundle_no_docker.sh  # 无 Docker 方案：wheel + dist + 源码 + systemd 单元
```

### 5. 配置模型（可选，不做也能跑）

复制 `prototype/.env.example` 为 `prototype/.env` 并填 key（留空 = mock 模式）。详见[六、系统配置 → 模型接入](#模型接入)。

---

## 三、核心概念与告警旅程

### 术语对照表

| 生物/神经术语 | 安全术语 | 实际功能 |
|---|---|---|
| 丘脑 Thalamus | 原始告警 | 原始告警流视图（含被抑制项 + 抑制原因） |
| 海马体 Hippocampus | 关联分析 | 全局实体关系图 + 事件下钻 |
| 杏仁核 Amygdala | 初筛模型 | 对单条告警快速打分（可疑/置信度/理由） |
| 前额叶 / 系统2 | 深度分析模型 | 对升级案件做结构化深度研判报告 |
| 黑板 Blackboard | 全局工作区 | 统一 Event 模型 + 显著性/相关性加权 |
| 固有免疫 Innate | 检测规则（黑名单） | 已知坏签名 → 置信度 0.95 秒拦，不调模型 |
| 免疫耐受 Tolerance | 白名单 | 已知好签名 → 降级（静默但不丢弃） |
| 签名 Signature | 告警形状 | 掩码 IP/hash/数字，提取稳定的告警模板 |
| 神经调质 Knob | 风险等级 | 风险预设，映射到抑制线/顶出线/预算 |
| 风险分 Risk | 风险评分 | 资产价值 × 攻击得逞 × 危害，替代置信度排序决定顶出 |
| 睡眠巩固 Consolidate | 夜间记忆固化 | 定期蒸馏当日案件为可检索记忆 |

### 一条告警的完整旅程

```
① 免疫耐受（白名单）  → 命中「已知好」签名，降级（留痕，不丢弃）
② 固有免疫（黑名单）  → 命中「已知坏」签名，置信度 0.95 秒拦，不调模型
③ 杏仁核（快模型）    → 逐条打分，低于抑制线 → 降级
④ 黑板（相关性加权）  → 弱信号拼链，计算案件强度
⑤ 实体图（连通分量）  → 关联同一实体的告警聚成「案件」
⑥ 前额叶（系统2）     → 案件顶出后深度研判，出结构化报告
⑦ 分析师判定         → 误报写白名单 / 真阳写黑名单
⑧ 夜间巩固           → 当天案件蒸馏成检索记忆（供系统2 RAG）
```

**设计原则**：**抑制 = 降级 + 留痕，永不静默丢弃**。每条被压掉的告警都能在「丘脑」页看到原因，必要时一键「放回」。

### 关键概念详解

- **签名 Signature**：把一条告警的「形状」抽成稳定模板——掩码掉会变的 token（IP/哈希/独立数字/UUID），保留语义 token（主机名/账号/描述词）。`svc_backup 登录跳板机` 和 `svc_backup 登录数据库` 是不同签名；`资产A 的端口扫描` 和 `资产B 的端口扫描` 也是不同签名（asset 不掩码）。
- **案件强度**：`max(各告警置信度) + min(链路加成上限, 链路加成 × (告警数-1))`。告警越多、置信度越高，案件强度越高（用于展示排序）。
- **风险分 Risk**：`资产价值 × 攻击得逞 × 危害`，三个维度都归一化到 0-1。资产价值来自「内部资产清单」匹配（普通 0.3 / 重要 0.6 / 核心 1.0，未匹配默认 0.3）；攻击得逞（阻断/失败 0 / 成功 0.7 / 失陷 1.0，缺失保守取 1.0）；危害（低 0.25 / 中 0.5 / 高 0.75 / 危急 1.0，缺失取 0.5）。缺失维度标记 `risk_incomplete`，不硬编高分。
- **顶出 Escalate**：案件风险分 ≥ 风险阈值（默认 0.3）→ 唤醒系统2 深度研判。**风险分替代旧版置信度排序**决定顶出优先级。

---

## 四、页面功能与使用

### 1. 看板 Dashboard

**功能**：全局态势一览 + 降噪效果展示。

**界面元素**：

| 元素 | 说明 |
|---|---|
| KPI 卡片（两排 8 个） | 案件、告警（上板/抑制）、被抑制、深度分析、实体、攻击链、白名单、规则；点击可跳转对应页面 |
| 告警降噪漏斗 | 告警 → 归案 → 深度分析，三行条形图 + 降噪率 |
| 流量趋势 | 近 24 小时 / 7 天 / 30 天的告警流量曲线 |
| 导出报告按钮 | 打开报告导出对话框 |

**操作**：
- 点 KPI 卡片跳转（案件→分诊，告警/被抑制→丘脑，实体→海马体，白名单/规则→免疫）。
- 切流量趋势的时间范围（24h/7d/30d）。
- 「导出报告」→ 见下「报告导出」。

**报告导出**（看板/案件详情都能触发）：
- **时间范围**：全部时间 / 最近 24 小时 / 最近 7 天 / 自定义（起止时间）。
- **筛选**：来源、定性（结论）、状态。
- **格式**：Word(.docx) / Markdown(.md) / HTML(.html)。

### 2. 分诊 Triage（案件队列）

**功能**：案件待办队列，逐个或批量处置。

**界面元素**：

| 元素 | 说明 |
|---|---|
| 搜索框 | 按案件 ID / 标题 / 实体搜索 |
| 只看待处理 | 勾选后只显示 `New / In Progress / On Hold` 状态的案件 |
| 状态筛选 | New / In Progress / On Hold / Resolved / Closed |
| 结论筛选 | 真阳性 / 可疑 / 误报 / 良性 / 证据不足 |
| 表格 | 勾选、案件ID、实体、严重度、状态、结论 |
| 批量标记误报 | 批量把勾选案件写白名单 |

**操作步骤**：
1. **筛选/搜索**：用搜索框、状态下拉、结论下拉、「只看待处理」缩小范围。
2. **查看案件**：点某行进入案件详情。
3. **批量标记误报**：
   - 勾选要处置的案件（已标记的案件复选框为灰，不可勾选）；
   - 在「批量处置理由」框填理由（可选，会写进记忆）；
   - 点「批量标记误报」→ 弹二次确认 → 确认后每个案件：写白名单 + 状态置 Closed + 记反馈（含理由）。

**严重度**：按案件强度分高（≥1.0）/ 中（≥0.8）/ 低（其余）。

### 3. 案件详情 CaseDetail

**功能**：单个案件的完整信息与处置入口。

**界面分区**：

| 分区 | 内容 |
|---|---|
| 头部 | 案件 ID、强度、告警数 |
| 攻击链 | 按时间串起的告警步骤，点击节点在实体图高亮 |
| 告警时间线 | 逐条告警 + 置信度 + 单条误报/真阳性标记按钮 |
| 实体图 | 案件内实体关系图（双向高亮），点实体反查关联案件 |
| AI 调查报告 | 系统2 生成的结构化报告（见下） |
| 处置操作栏 | 处置理由输入、结论下拉、保存结论、关闭/重启、外发、导出 |

**AI 调查报告结构**（ReportView 展示）：

| 字段 | 说明 |
|---|---|
| 定性 verdict + 置信度 confidence | 报告结论 |
| 摘要 digest | 一句话概括 |
| 证据 evidence | `事实 → 结论` 列表 |
| 攻击链 attack_chain | `阶段：描述` 列表 |
| IOC | 失陷指标（值 + 上下文） |
| 待查 unknowns | 还需人工确认的项 |
| 处置建议 remediations | 建议操作 |

**处置操作详解**：

| 操作 | 效果 |
|---|---|
| 保存结论 = 误报 | 该案签名写白名单（以后同形状降级），状态置 Closed |
| 保存结论 = 真阳性 | 该案签名写黑名单（以后同形状秒拦），状态置 Closed |
| 保存结论 = 可疑 / 良性 | 仅记录结论，不改状态 |
| 保存结论 = 证据不足 | 状态自动置「挂起 On Hold」（仍算待处理） |
| 关闭案件 | 状态置 Closed（终态） |
| 重启 | 已关闭案件重新打开，状态置 In Progress（按钮高亮显示） |
| 外发 | 手动推送到配置的 webhook 目标 |
| 导出报告 | 导出该案报告 |

**单条告警标记**：时间线里每条告警旁有「误报/真阳性」按钮，单独把该条告警写白名单/黑名单。

### 4. 丘脑 Thalamus（原始告警）

**功能**：查看所有原始告警（**含被抑制的**），是「放回」误压告警的入口。

**界面元素**：

| 元素 | 说明 |
|---|---|
| 搜索框 | 按 raw / 主体 / 类型搜索 |
| 来源下拉 | 按来源（天眼/HIDS/WAF…）过滤 |
| 抑制状态下拉 | 全部（含被抑制）/ 仅上板 / 仅被抑制 |
| 排序 | 按时间 / 按置信度 |
| 告警流 | 每条：时间、来源/类型、置信度、标签（被抑制/固有免疫）、raw、抑制原因 |
| 放回按钮 | 仅被抑制的告警显示 |
| 决策留痕侧栏 | 「为什么没深想 / 为什么被拦」的审计记录 |

**操作步骤**：
1. **筛选**：用来源/抑制状态/搜索/排序定位告警。
2. **看抑制原因**：被抑制的告警显示「原因：免疫耐受：已知好 / 杏仁核低置信度（x < 抑制线）/ 固有免疫秒拦」等。
3. **放回**：点「放回」→ 该信号强制上板、归入案件、触发系统2 深想，并跳转到对应案件。
4. **看决策留痕**：右侧边栏显示 `[时间] action · entity` + 变更详情，全程可追溯。

### 5. 免疫 Immune（规则库）

**功能**：管理白名单（免疫耐受）与黑名单（固有免疫）。

| 卡片 | 说明 | 操作 |
|---|---|---|
| 免疫耐受（已知好→降级） | 按签名精确匹配；命中即降级，连杏仁核都不叫 | 逐条删除 × / 清空 |
| 固有免疫（已知坏→秒拦） | 命中即边缘秒拦（置信度 0.95），前额叶不醒 | 逐条删除 × / 清空 |

**签名长什么样**：形如 `天眼|ssrf|<IP> 探测云元数据 <IP>`——IP/哈希/数字被掩成占位符，保留语义。这些签名主要由「标记误报/真阳性」自动写入，也可在此手动清理。

### 6. 海马体 Hippocampus（关联分析）

**功能**：全局实体关联图，看清告警之间的关联。

**界面元素**：

| 元素 | 说明 |
|---|---|
| 视图模式 | 全部 / IP / 实体（主机账号） |
| 节点类型 | 实体（主机/账号，蓝）、IP（绿）、文件哈希（红）、域名（橙） |
| 图 | G6 图，节点=实体，边=共同出现的告警 |
| 事件下钻 | 点节点或边，下钻该实体/该关联的告警列表 |
| 来源过滤 / 排序 | 下钻事件按来源过滤、按时间/置信度排序 |

**操作**：切换视图模式 → 点节点/边 → 下方显示关联事件 → 点事件可跳转到对应案件。

### 7. 设置 Settings

| 区块 | 功能与操作 |
|---|---|
| 风险旋钮 | 点「宽松/正常/保守/战时」切换档位（见[六、系统配置 → 风险旋钮](#风险旋钮四档)） |
| 模型模式 | 自动（有 key 用真实，否则 mock）/ 真实 / Mock（零成本）；免重启立即生效 |
| 数据接入 | 显示 syslog 监听地址/端口、模型信息；「上传告警文件」导入 JSONL/JSON/CSV |
| 健康状态 | DB 告警/案件数、syslog 监听状态、当前旋钮、模型模式、最近入库时间；「立即夜间巩固」「清空数据库」 |
| 界面术语 | 生物术语 / 安全术语切换 |

**清空数据库**：清空告警/案件/报告等（**注意**：不清记忆，记忆在「高级设置 → 记忆管理」里清）。

### 8. 高级设置 AdvancedSettings

进入方式：设置页点「高级设置 →」。左侧是分区导航，右侧是当前分区的表单。**大部分配置「保存」即生效**（syslog 地址/端口改后需重启后端）。

各分区详见[六、系统配置](#六系统配置)。

### 9. 用户 Users

| 功能 | 说明 |
|---|---|
| 修改密码 | 改自己的密码 |
| 注册用户 | 管理员建号（用户名/密码/角色） |
| 改角色 | admin ↔ user |
| 重置密码 | 管理员给某用户重置密码 |
| 删号 | 删除用户 |
| 配权限 | 给 user 逐项勾选 triage/config/maintenance/users 权限 |

详见[七、权限管理](#七权限管理)。

---

## 五、数据接入

平台支持**多通道 + 多来源**接入，五条通道汇聚到同一条处理流水线。

### 通道 1：syslog 接收（实时流式）

- 监听 `UDP/TCP :5514`，解析 RFC3164 / RFC5424 标准 syslog。
- 默认只绑 `127.0.0.1`（仅本机）；要收远程设备的日志，在「高级设置 → 数据接入（syslog）」把监听地址设为 `0.0.0.0`，并放行端口（Docker 需在 `docker-compose.yml` 取消 5514 端口映射的注释）。
- 把 rsyslog / 安全设备转发到该地址即可实时接入。

### 通道 2：HTTP JSON API（机器/脚本流式）

```
POST /api/ingest
Header: X-API-Token: <token>
Body: {"signals": [...], "knob": "正常"}
```

单条信号（signal）字段：

| 字段 | 说明 | 必填 |
|---|---|---|
| `time` | 事件时间 | 是 |
| `source` | 来源标签（调用方自报，如「天眼」） | 是 |
| `asset` | 资产/主体（主机名/账号） | 是 |
| `type` | 告警类型 | 是 |
| `raw` | 原始描述 | 是 |

示例：

```bash
curl -X POST http://127.0.0.1:8000/api/ingest \
  -H "X-API-Token: <token>" -H "Content-Type: application/json" \
  -d '{"signals":[{"time":"2026-08-28 10:00:00","source":"天眼","asset":"web-01","type":"ssrf","raw":"探测云元数据 169.254.169.254"}]}'
```

> 未配置 token 时 `/api/ingest` 拒绝接入（fail-closed）。

### 通道 3：文件上传（批量导入）

「设置 → 数据接入 → 上传告警文件」，支持 `.jsonl / .json / .csv`，逐条增量入库（不重置）。

### 通道 4：CLI 批量（离线全量）

```bash
cd backend && python3 ingest.py ../prototype/data/sample.jsonl --knob 保守
```

读信号文件 → 跑管道 → 落库（**会重置库再入库**）。

### 通道 5：Filebeat 文件日志采集（Kafka 中转）

安全设备把日志写到本机文件，Filebeat 尾部采集 → Kafka → 后端消费，与其它通道走同一条流水线。适合日志量大、设备只落盘不推 syslog 的场景。

- **文件命名约定**：`<类型>-YYYY-MM-DD-HH.log`，如 `ips-2026-09-20-18.log`、`webids-2026-09-20-12.log`、`waf-2026-09-21-11.log`、`hids-…`。
- **来源识别**：靠 Filebeat 的 `fields.source`（`tianyan` / `waf` / `hids`）映射到「天眼 / WAF / HIDS」，再走对应来源的解析规则。
- **日志目录挂载**：`docker-compose.yml` 里 `TIANYAN_LOG_DIR` / `WAF_LOG_DIR` / `HIDS_LOG_DIR`（默认 `/wls/applogs/rtlog/sec-terminal-linux/{tianyanlog,waflog,hidslog}`），改路径在 `.env` 设这三个变量。
- **Kafka 连接**：`NEUROIMMUNE_KAFKA_BOOTSTRAP_SERVERS`（默认 `kafka:9092`）、`NEUROIMMUNE_KAFKA_TOPIC`（默认 `neuroimmune-logs`）、`NEUROIMMUNE_KAFKA_GROUP`（默认 `neuroimmune`）。

### 来源识别（syslog 专属）

syslog 进来后按「来源映射」自动识别是哪个系统发的，判定优先级：

```
ip（来源地址）> tag（应用名）> hostname（主机名）> facility（RFC 编码）
```

- 均为大小写不敏感的子串匹配；`ip` 可为完整地址或网段前缀（如 `10.20.`）。
- 映射在「高级设置 → 数据接入（syslog）→ 来源映射」编辑，四组键值：`ip` / `tag` / `hostname` / `facility`。
- **设备需发标准 RFC3164/5424 syslog**；非标准格式（裸 JSON、无 PRI/时间戳）会被丢弃。
- 未命中任何映射 → 落到 facility 内置默认（认证/系统/审计/其他），这些不显示在「已配置来源」，但会在丘脑看到。
- 有中转 rsyslog 时，`ip` 判定用的是**中转机 IP**，不是原始设备 IP。

### 来源解析（精确实体）

「高级设置 → 数据接入 → 来源解析」可为每个来源配置解析规则，解决「从告警文本里精确抽取实体」（消除版本号误抽等）：

1. 填**来源名**（如「天眼」）+ 贴**样本日志**（2~5 条，每行一条）。
2. 点「生成解析配置」→ 系统用模型生成规则，显示**可编辑的预览**。
3. 确认后「保存解析配置」。
4. 之后该来源的日志按规则**确定性解析**、精确实体拼链。也可点规则条目查看/编辑/启停/删除完整 JSON。

### 已配置来源现状

「高级设置 → 数据接入（syslog）→ 已配置来源」列出你在来源映射里配置的所有来源标签，以及各自的**告警数 / 最近入库时间**。某来源一直显示「暂无数据」，多半是设备数据没匹配上映射（去丘脑看实际 `source`）。

---

## 六、系统配置

### 模型接入

「高级设置 → 模型接入」字段：

| 字段 | 说明 | 默认 |
|---|---|---|
| 系统1 API key | 杏仁核（便宜模型）key | 空 = mock |
| 系统1 Base URL | OpenAI 兼容端点 | `https://api.deepseek.com/v1` |
| 系统1 模型名 | 杏仁核模型 | `deepseek-chat` |
| 系统2 API key | 前额叶（深想）key | 空 = 复用系统1 |
| 系统2 Base URL | 深想端点 | 空 = 复用系统1 |
| 系统2 模型名 | 深想模型 | `deepseek-reasoner` |
| 温度 temperature | 采样温度 | 0.0（推理模型不支持则不发送） |
| 超时 timeout | 请求超时（秒） | 120 |

> 任何 OpenAI 兼容端点都行（DeepSeek / OpenRouter / Groq / Ollama / vLLM）。**零 key 也能跑通全流程**（内置 MockClient 用关键词规则替代，见下「检测调参」）。这些字段也可用 `prototype/.env` 的环境变量兜底。

### 风险旋钮（四档）

「设置 → 风险旋钮」或「高级设置 → 阈值配置」。三个旋钮含义：

- **抑制线 suppress_below**：杏仁核置信度低于此值 → 降级（静默留痕）。
- **顶出线 escalate_above**：案件强度达到此值 → 唤醒系统2 深想。
- **深度预算 budget**：每窗口最多唤醒多少次贵模型（控成本）。

| 档位 | 抑制线 | 顶出线 | 预算 | 适用 |
|---|---|---|---|---|
| 宽松 | 0.75 | 0.85 | 1 | 只留最可疑的，最少深想 |
| 正常 | 0.55 | 0.75 | 2 | 默认 |
| 保守 | 0.40 | 0.62 | 3 | 多留多看 |
| 战时 | 0.25 | 0.45 | 99 | 宁可多报，全员深想 |

四档数值可在「高级设置 → 阈值配置」逐档覆盖。

### 频率降级

「高级设置 → 频率降级」。当某资产某类型告警在时间窗内出现次数过多 → 视为业务误报自动降级：

| 字段 | 说明 | 默认 |
|---|---|---|
| 时间窗 window | 统计窗口（秒） | 3600（1 小时） |
| 频次阈值 threshold | 超过该次数触发降级 | 10 |
| 折扣系数 demote | 置信度乘以此系数降级 | 0.4 |

### 前额叶唤醒门槛

「高级设置 → 前额叶唤醒门槛」，控制系统2（贵模型）何时被唤醒：

| 字段 | 说明 | 默认 |
|---|---|---|
| 单信号地板值 single_signal_floor | 单条告警置信度 ≥ 此值才允许单独唤醒（否则要等拼链） | 0.98 |
| 预算窗口 budget_window | 预算计数窗口（秒） | 3600 |

规则：单信号案件默认不醒（除非置信度 ≥ 地板值），要等拼链或确凿单点 IOC；窗口内唤醒的不同案件数超预算则拦下并记审计。

### 检测调参

「高级设置 → 检测调参」：

| 字段 | 说明 | 默认 |
|---|---|---|
| 固有免疫置信度 innate_conf | 黑名单秒拦时给的置信度 | 0.95 |
| 链路加成 chain_bonus | 案件里每多一条告警的强度加成 | 0.10 |
| 链路加成上限 chain_cap | 加成封顶 | 0.30 |
| 重分析触发 grew | 案件告警净增 ≥ 此数时重跑系统2 | 2 |
| RAG 检索条数 rag_limit | 系统2 检索历史记忆/反馈条数 | 5 |
| 放回置信度 restore_conf | 「放回」信号上板时的置信度 | 0.90 |
| 白名单 TTL tolerance_ttl_days | 白名单过期天数（0=永久） | 90 |
| 杏仁核并发 judge_concurrency | 小模型并发上限 | 5 |
| 系统2并发 deep_concurrency | 大模型并发上限 | 2 |
| mock 规则关键词 | 关键词 → 权重表（Mock 模式杏仁核的规则） | 内置默认 |
| mock 未命中分 no_hit | 未命中任何关键词的置信度 | 0.15 |
| mock 基础分 base | 命中关键词的起始分 | 0.32 |
| mock 上限 ceiling | 置信度封顶 | 0.72 |
| mock 判定线 cutoff | ≥ 此值判为可疑 | 0.50 |

> Mock 模式（没配模型 key 时）杏仁核用「关键词权重」规则打分：命中关键词累加权重（封顶 ceiling），置信度 ≥ cutoff 判可疑。可在「检测调参」里调关键词与权重，让 mock 也贴近真实。

### 风险模型（风险分）

案件是否顶出由「风险分」决定（替代旧版按置信度排序）。风险分 = 资产价值 × 攻击得逞 × 危害：

| 字段 | 说明 | 默认 |
|---|---|---|
| 风险阈值 risk_threshold | 案件风险分 ≥ 此值才考虑顶出 | 0.30 |

三个维度的取值（归一化到 0-1）：
- **资产价值 criticality**：普通 0.3 / 重要 0.6 / 核心 1.0，来自「内部资产清单」匹配，未匹配默认 0.3
- **攻击得逞 attack_result**：阻断/失败 0.0 / 成功 0.7 / 失陷 1.0，缺失保守取 1.0（标 `risk_incomplete`）
- **危害 severity**：低 0.25 / 中 0.5 / 高 0.75 / 危急 1.0，缺失取 0.5（标 `risk_incomplete`）

### 内部资产清单

「高级设置 → 资产清单」，配置「我们自己的资产」：角色 + IP/CIDR/域名 + 敏感度。命中资产的 IP/域名会在研判时标注 criticality，用于风险分；角色为「出口IP」的实体命中会触发出口IP降噪（置信度 ×0.85，内部出网温和降噪）。

| 字段 | 说明 |
|---|---|
| value | IP / CIDR 网段 / 域名（IP 最长前缀优先匹配） |
| role | 角色标签（如「出口IP」「内网网段」「生产网段」） |
| criticality | 敏感度：normal / important / critical |

支持「批量导入」：每行 `值 角色`，如 `1.2.3.4 出口IP`、`10.0.0.0/8 内网网段`。

### 数据接入（syslog）

「高级设置 → 数据接入（syslog）」：

| 字段 | 说明 | 默认 |
|---|---|---|
| syslog 地址 | 监听地址 | 127.0.0.1（对外收改 0.0.0.0） |
| syslog 端口 | 监听端口 | 5514 |
| 巩固间隔 consolidate_interval | 夜间巩固间隔（秒） | 21600（6 小时） |
| 告警保留天数 retention_alert_days | 超期先归档再删 | 30 |
| 案件保留天数 retention_case_days | 案件/报告保留 | 180 |
| API token | 机器入库 `X-API-Token` | 空 = 拒绝接入 |

> syslog 地址/端口改后需**重启后端**；巩固间隔、保留天数、token 下个周期生效。

### 案件外发（Webhook）

「高级设置 → 案件外发（Webhook）」，把需要深度处理的案件自动 POST 给下游（SOAR/SIEM/工单/通知）：

| 字段 | 说明 |
|---|---|
| 名称 name | 目标标识 |
| URL | 下游地址 |
| Token | 可选 Bearer 认证 |
| 触发 trigger | 顶出即推(escalated) / 全部(all) / 仅手动(manual) |
| 外发字段 fields | 勾选要发的字段（报告子字段用 `report.xxx`） |
| 自定义请求头 headers | 键值对 |
| 请求体模板 body | Jinja2 模板，留空 = 默认固定 JSON 信封 |

触发时机：案件顶出深析后（escalated）、分析师标记误报/真阳性后（disposition）、手动外发（case_pushed）。可「测试」单条目标。手动外发在案件详情页「外发」按钮。

### 记忆管理

「高级设置 → 记忆管理」，两类记忆的查看/逐条删/清空：

| 类型 | 文件 | 来源 |
|---|---|---|
| 睡眠巩固记忆 | memory.jsonl | 夜间巩固蒸馏 |
| 处置反馈 | feedback.jsonl | 标记误报/真阳性、频率降级 |

判错的记忆可在此删除或清空，避免污染后续研判的 RAG 上下文。

---

## 七、权限管理

- **认证**：JWT（HS256，24h）+ bcrypt 密码哈希。
- **角色**：`admin`（隐式全权限）/ `user`（按权限列表判断）。

四类细粒度权限：

| 权限键 | 含义 | 覆盖路由 |
|---|---|---|
| `triage` | 分诊动作 | 标记误报/真阳、放回、改案件、外发、批量误报、免疫规则增删清 |
| `config` | 配置写 | 旋钮/模式/模型/阈值/接入/来源映射/webhook/频率/门控 |
| `maintenance` | 维护 | 清库、夜间巩固、文件上传 |
| `users` | 用户管理 | 建号/删号/改角色/重置密码/配权限 |

- **读接口**（所有 GET）登录即可看，不设 `view` 键。
- 新建 `user` 默认 `["triage"]`（能看 + 能分诊）；admin 在「用户」页逐人勾选权限。
- admin 隐式全权限，避免「把自己权限关掉锁死」。

---

## 八、运维与维护

### 健康状态

「设置 → 健康状态」：DB 规模、syslog 监听状态（UDP/TCP/worker 存活）、当前旋钮、模型模式、最近入库时间。

### 夜间巩固（睡眠巩固）

定时（默认 6 小时，高级设置可改）把当天案件用深想模型蒸馏成 `{摘要, 手法, 误报模式}`，追加到 memory.jsonl，供后续系统2 研判检索（RAG）。可在「设置 → 立即夜间巩固」手动触发。

> **注意**：夜间巩固只固化「检索记忆」，**不回写免疫规则**——规则只由分析师「标记真阳性」写入，避免未确认的误报被永久写进黑名单。

### 记忆管理

见[六、系统配置 → 记忆管理](#记忆管理)。判错的记忆/反馈可删除或清空。

### 备份与恢复

- `./scripts/backup.sh` 备份；`./scripts/restore.sh` 恢复。
- 数据（SQLite + 状态 JSON + 记忆 + 来源映射）集中在数据目录，Docker 下挂载在命名卷 `neuroimmune-data`。

### 保留策略

告警超期（默认 30 天）先归档到 `archive/` 再删除；案件/报告保留更久（默认 180 天）。

---

## 九、常见问题 FAQ

**Q1：为什么「已配置来源」显示「暂无数据」？**
库里还没有该来源的告警；或设备数据没匹配上来源映射。去「丘脑」看实际进来的 `source` 是什么，回来改映射。

**Q2：标记误报后，未来同样的攻击会不会被漏掉？**
系统按「签名/形状」匹配白名单，无法区分「同形状的运维操作」和「复刻它的攻击」。但有三层兜底：① 降级留痕（丘脑可见、可放回，不静默丢弃）；② 白名单 TTL 到期自动失效（默认 90 天，可调）；③ 签名细粒度（区分资产/账号/主机名）。

**Q3：「证据不足」和「关闭」有什么区别？**
「证据不足」会把案件置为「挂起 On Hold」，仍算待处理（还在待办里）；「关闭」是终态，但可「重启」重新打开。

**Q4：状态为什么是英文？结论为什么是中文？**
状态字段（New/In Progress/On Hold/Resolved/Closed）保持英文；结论（verdict：真阳性/可疑/误报/良性/证据不足）显示中文。

**Q5：如何让平台接收远程安全设备的日志？**
① 「高级设置 → 数据接入」把 syslog 地址设为 `0.0.0.0`（Docker 需映射 5514 端口）；② 防火墙放行 5514；③ 设备端配置 syslog 转发到 `平台IP:5514`；④ 在「来源映射」里配好设备 IP/TAG/主机名 → 标签。

**Q6：清空数据库会清掉记忆吗？**
不会。「清空数据库」只清告警/案件/报告等，记忆在「高级设置 → 记忆管理」单独清。

**Q7：批量标记误报为什么有的案件选不了？**
已标记（有结论）的案件不可再被批量选中，避免重复标记。如需重新处理，进案件详情单独改结论。
